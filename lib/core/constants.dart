import 'package:flutter/material.dart';

class AppColors {
  // Primary palette
  static const primary = Colors.deepPurple;
  static const secondary = Colors.teal;
  static const accent = Colors.orangeAccent;
  // backgrounds
  static const bgLight = Colors.cyanAccent;
  static const bgDark = Colors.blueGrey;
  static const surface = Colors.black;
  // Task priority colors
  static const high = Colors.red;
  static const medium = Colors.yellow;
  static const low = Colors.blue;
}

class AppSizes {
  static const paddingS = 8.0;
  static const paddingM = 16.0;
  static const paddingL = 24.0;
  static const radiusM = 12.0;
  static const radiusL = 20.0;
}
