# Copy to .env for local dev OR pass via flutter run:
# flutter run --dart-define=GEMINI_API_KEY=your_key --dart-define=OPENAI_API_KEY=your_key
#
# Prefer saving keys in app Settings for development.
# Production: use Firebase Cloud Functions so keys stay server-side.

GEMINI_API_KEY=
OPENAI_API_KEY=
